# Quality gate — runs on every push/PR to main before deploy workflows ship a build.
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Create dart_defines.json
        run: |
          cat > dart_defines.json <<EOF
          {
            "SUPABASE_URL": "https://example.supabase.co",
            "SUPABASE_ANON_KEY": "placeholder",
            "EMAILJS_SERVICE_ID": "service_ib2e5fk",
            "EMAILJS_TEMPLATE_ID": "template_l4ieguu",
            "EMAILJS_PUBLIC_KEY": "dGt7FQDgGufoc7sVy",
            "SUPER_ADMIN_EMAIL": "ravivtu12345@gmail.com",
            "MAPS_API_KEY": ""
          }
          EOF

      - run: flutter pub get
      - run: flutter analyze --no-fatal-infos
      - run: flutter test
